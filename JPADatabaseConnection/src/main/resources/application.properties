spring.application.name=JPADatabaseConnection
spring.datasource.url=jdbc:mysql://localhost:3306/sql_practice
spring.datasource.username=root
spring.datasource.password=123456

# Auto-create or update database tables -  need
spring.jpa.hibernate.ddl-auto=update 

 # Show SQL queries in the log = not needed Optional
spring.jpa.show-sql=true


#optional Optional (Spring Boot can auto-detect)
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect